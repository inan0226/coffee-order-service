---
name: spring-assignment-reviewer
description: coffee-order-service 과제 제출 전 리뷰에 사용한다. 요구사항 충족, Spring API 설계, 트랜잭션 안전성, 포인트 결제 정확성, Mock 이벤트 전송, 최근 7일 인기 메뉴 정확성, 테스트 누락을 점검한다.
---

# Spring 과제 리뷰 스킬

## 리뷰 순서

1. 요구사항
   - 커피 메뉴 목록 조회 API가 있다.
   - 포인트 충전 API가 있다.
   - 커피 주문 및 포인트 결제 API가 있다.
   - 성공한 주문은 Mock 데이터 플랫폼 이벤트를 전송한다.
   - 인기 메뉴 API는 최근 7일 기준 TOP 3를 반환한다.

2. 정확성
   - 포인트는 음수가 될 수 없다.
   - 잘못된 충전 금액은 거절된다.
   - 존재하지 않는 메뉴는 처리된다.
   - 실패한 주문은 주문을 생성하지 않는다.
   - 실패한 주문은 이벤트를 발행하지 않는다.
   - 인기 메뉴는 성공 주문만 집계한다.

3. 설계
   - controller는 얇게 유지된다.
   - service가 비즈니스 규칙을 가진다.
   - request/response DTO를 사용한다.
   - Entity를 controller 응답으로 직접 노출하지 않는다.
   - 시간 범위 로직은 테스트 가능하다.

4. 테스트
   - service 테스트가 핵심 규칙을 검증한다.
   - 필요한 경우 API request/response 형태를 controller 테스트로 검증한다.
   - Mock 이벤트 전송을 검증한다.
   - 최근 7일 경계값을 테스트한다.

## 리뷰 출력 형식

아래처럼 간결하게 출력한다.

```text
요구사항 충족:
- ...

위험 요소:
- ...

테스트:
- ...

다음 추천 수정:
- ...
```

## 제출 게이트

아래 조건을 만족하지 못하면 과제가 완료됐다고 말하지 않는다.

- 네 가지 요구사항 그룹이 모두 구현되어 있다.
- 전체 테스트가 통과한다.
- 의도적으로 남긴 제한사항이 있다면 명확히 보고한다.
